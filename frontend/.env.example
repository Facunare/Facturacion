# En desarrollo local podés dejar esto vacío (Vite usa el proxy de vite.config.ts hacia localhost:4000).
# En producción, poné la URL completa de tu backend desplegado, terminada en /api:
VITE_API_URL=https://tu-backend.onrender.com/api
